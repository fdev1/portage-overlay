--- a/vboxdrv/linux/SUPDrv-linux.c	2013-02-08 08:16:55.000000000 -0500
+++ b/vboxdrv/linux/SUPDrv-linux.c	2015-05-14 13:13:36.000000000 -0400
@@ -222,7 +222,9 @@
 
 DECLINLINE(RTUID) vboxdrvLinuxUid(void)
 {
-#if LINUX_VERSION_CODE >= KERNEL_VERSION(2, 6, 29)
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(3, 19, 0)
+    return current->cred->uid.val;
+#elif LINUX_VERSION_CODE >= KERNEL_VERSION(2, 6, 29)
     return current->cred->uid;
 #else
     return current->uid;
@@ -231,7 +233,9 @@
 
 DECLINLINE(RTGID) vboxdrvLinuxGid(void)
 {
-#if LINUX_VERSION_CODE >= KERNEL_VERSION(2, 6, 29)
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(3, 19, 0)
+    return current->cred->gid.val;
+#elif LINUX_VERSION_CODE >= KERNEL_VERSION(2, 6, 29)
     return current->cred->gid;
 #else
     return current->gid;
@@ -240,7 +244,9 @@
 
 DECLINLINE(RTUID) vboxdrvLinuxEuid(void)
 {
-#if LINUX_VERSION_CODE >= KERNEL_VERSION(2, 6, 29)
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(3, 19, 0)
+    return current->cred->euid.val;
+#elif LINUX_VERSION_CODE >= KERNEL_VERSION(2, 6, 29)
     return current->cred->euid;
 #else
     return current->euid;
